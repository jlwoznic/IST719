my.data <- c (12, 22, 53, 54)
plot(my.data, 16, replace = TRUE)
plot(sample(my.data, 16, replace = T))


#what does sample do? plot(sample(my.data, 16, replace = T))

my.var = c(2,4,6)

Plot(may.var)
plot (my.var)

#In R, the operators “|” and “&” indicate the logical operations OR and AND
#The lapply and sapply both apply a specified function to each element of a list. The former returns a list object and the latter a vector when possible.
#Definition of List: An ordered collection of objects (components). A list allows you to gather a variety of (possibly unrelated) objects under one name.

